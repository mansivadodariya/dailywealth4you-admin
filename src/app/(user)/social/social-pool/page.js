import SocialPoolManagement from '@/renderning/(user)/social/social-pool';

export const metadata = {
  title: 'Social Pool Management | DailyWeath4U',
};

export default function SocialPoolPage() {
  return <SocialPoolManagement />;
}
